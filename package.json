{
  "name": "alberto-trujillo-portfolio",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=alberto-portfolio --branch=main"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.130.0"
  }
}
